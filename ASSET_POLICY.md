# Project Curse 자산 정책

이 문서는 기록글의 원본 이미지·복구 이미지·연출용 이미지를 서로 혼동하지 않도록 관리하는 기준이다.

## 자산 등급

- **ORIGINAL**: 사용자 제공 원본. 색상 보정이나 재구성으로 덮어쓰지 않는다.
- **STABILIZED**: 원본의 노이즈·명암·크롭을 열람용으로 보정한 파생본. 원본과 나란히 보관한다.
- **RECONSTRUCTED**: 소실 장면을 설정에 맞춰 재현한 이미지. 기록 화면에 반드시 `복원 추정본`이라고 표시한다.
- **UNVERIFIED**: 기존 기록에 포함돼 있으나 원본 계보·변환 이력이 아직 확인되지 않은 이미지. 원본으로 단정하지 않고 `출처 대조 대기`로 표시한다.
- **INTERFACE**: 지도 배경, 단말 텍스처, 아이콘 등 UI 전용 자산. 기록 증거물처럼 소개하지 않는다.

## 파일과 메타데이터

원본은 `assets/resources/originals/`, 파생본은 `assets/resources/derived/`, UI 자산은 해당 기능 폴더에서 관리하는 것을 기본으로 한다. 새 파일 이름은 `record-id_sequence_kind.ext` 형식을 권장한다. 예: `immortality-860201_005_original.webp`.

기록 이미지에는 가능한 경우 다음 정보를 함께 둔다.

- 기록 ID와 장면 순서
- 등급: ORIGINAL / STABILIZED / RECONSTRUCTED
- 출처 또는 제공자
- 촬영·제작 시점(알 수 없으면 `UNKNOWN`)
- 파생본인 경우 원본 파일명
- 화면에 표시할 대체 텍스트와 캡션

## 보호 원칙

`Cults_871104`와 `Immortality_860201`의 원문·기존 원본 이미지는 보호 대상이다. 교체가 필요하면 원본을 보존한 상태에서 새 파생본을 추가하고, 화면에서는 어느 버전인지 명시한다. 생성형 이미지나 합성 이미지를 원본 기록으로 표기하지 않는다.

## 등록 절차

1. 원본을 별도 경로에 보존한다.
2. 등급과 출처를 정한다.
3. 파생본이면 원본과의 연결 정보를 남긴다.
4. 작은 화면과 큰 화면에서 크롭·캡션·대체 텍스트를 확인한다.
5. 보호 기록 해시와 패키지 검증을 통과한 뒤 공개 색인에 등록한다.

## 시각 증거 보관소 연결

`assets/js/data/visual-evidence-data.js`가 기록 화면의 시각 증거 등급과 비교 관계를 관리한다. 실제 원본이 새로 제공됐을 때는 기존 복원본이나 구형 사본을 덮어쓰지 않고 다음 순서로 연결한다.

1. 원본을 `assets/resources/originals/` 또는 출처가 보존되는 전용 하위 폴더에 추가한다.
2. 기존 자산 항목의 `comparison.src`에 원본 경로를 등록한다.
3. 원본에는 `ORIGINAL`, 열람 보정본에는 `STABILIZED`, 기존 합성·생성 자산에는 `RECONSTRUCTED`를 유지한다.
4. 비교 관계에 `SOURCE TO STABILIZED`, `SOURCE TO RECONSTRUCTION`, `LEGACY CROP TO SOURCE`처럼 실제 관계를 기록한다.
5. 데스크톱과 모바일 비교 화면에서 두 이미지의 크롭·색상·정보 손실을 확인한 뒤 기존 화면의 기본 노출 이미지를 변경한다.

원본이 없는 복원 추정본에는 비교 자료를 임의로 만들지 않는다. 이 경우 화면은 `ORIGINAL NOT REGISTERED` 상태를 유지한다.

## 반응형 열람 파생본

`assets/resources/responsive/`의 WebP 파일은 원본·보정본·복원본 등급을 새로 부여하는 증거 자산이 아니라 전송량을 줄이기 위한 열람 파생본이다. 출처 판단과 전체 화면 대조는 항상 `assets/resources/` 또는 `assets/resources/archive-enex/`의 보존 파일을 기준으로 한다.

- 반응형 파일은 원본을 덮어쓰지 않는다.
- 파일명 끝의 `-w480`, `-w960`은 출력 픽셀 폭을 뜻한다.
- `media-manifest.js`에는 원본 치수와 실제 존재하는 후보만 등록한다.
- 일반 문서·목록·영상 화면은 반응형 후보를 사용하고, 증거 확대·원본 비교는 보존 파일을 요청한다.
- 새 원본이 들어오면 먼저 출처 등급을 등록한 뒤 별도의 반응형 파생본을 만든다.
- 파생본 생성 후 보호 기록 해시, 파일 존재, 원본보다 작은 크기와 데스크톱·모바일 표시를 검증한다.
