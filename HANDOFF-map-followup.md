# 상황 관제 지도 후속 A-2 인계

- 사용자 결정: 2026-09-25 지도 여섯 기능.
- 작업 브랜치: `codex/map-followup`.
- 기준 커밋: `3083e9d` — 앞 작업 A의 지도·인물 연출 복원. 시작 시 작업 트리는 깨끗했다.
- 상태: 구현·검증 완료, 작업 트리에 미커밋 상태로 남김.
- 실제 Git 관리 경로는 `C:/Users/FORYOUCOM/OneDrive/문서/GitHub/project-curse/.git/worktrees/codex-atm-a`다. 현재 허용된 쓰기 루트 밖이며 `.git`은 읽기 전용이다. 이번 요청의 예외 규칙에 따라 커밋하지 않았다. Claude가 검토 후 커밋한다.

## 반영할 파일과 버전

| 파일·줄 | 변경 |
|---|---|
| `assets/app/js/screens/map.js:115` | 연도 상태, 날짜 연결 규칙, 1975~2042 막대와 재생, 부호 분류표 |
| `assets/app/js/screens/map.js:290` | 신뢰도 안개, 고정 크기 표식, SVG 이미지의 viewBox 갱신, 휠·포인터·키보드 조작 |
| `assets/app/js/screens/map.js:369` | 명시적으로 연결된 증거 찾기, 작은 사진 판, 알려진 감식 속성 |
| `assets/app/js/screens/map.js:391` | 관측점의 짧은 신호선, 드문 수신 끊김, 삼야 무응답 상태 |
| `assets/app/js/screens/map.js:426` | 세계·권역 지도에 연도·부호·사진·확대 기능 연결, 연도 미상 목록 분리 |
| `assets/app/js/screens/map.js:482` | 상세도 신뢰도 연결, 위험·통신·왜곡 표시도 해당 지점의 연도에 맞춤 |
| `assets/app/js/screens/map.js:642` | 작전 목록의 연도 구분 |
| `assets/app/js/screens/map.js:668` | 지점 상세에서 해당 권역으로 돌아가기 |
| `assets/app/js/screens/map.js:744` | 입력 리스너·기념일 감시 등록, hide에서 타이머·포인터·감시자 정리 |
| `assets/app/css/screens/map.css:132` | 지도 내부 터치 범위, 표식·안개 쌓임 순서, 연도·미리보기 배치, 정지 상태의 신호선 |
| `assets/js/data/map-screen-data.js:127` | 표시 설정과 안내문만 추가: 1.1초/년, 최대 6배, 신호 중단 간격·지속 시간 |
| `tools/verify-app.d/map.mjs:194` | 새 기능 실행 검사 7개. 기존 검사와 보호 조건 유지 |
| `index.html:34`, `index.html:124`, `index.html:143` | stamp가 갱신한 주소 해시 3개만 변경 |

주소 버전은 각각 `map.css?v=6.0.0-9749b149`, `map-screen-data.js?v=6.0.0-88c4c57d`, `map.js?v=6.0.0-20bae5ad`다. 사실 데이터·공용 셸·다른 화면·검증 본체·옛 앱은 변경하지 않았다.

## 구현 동작과 자료 한계

### 연도

- 첫 진입은 2042년이다. 막대는 실제 `input type=range`이며 `aria-valuetext`는 `YYYY년`으로 바뀐다. 방향키와 끌기 모두 기본 입력 동작을 쓴다.
- 직접 날짜·연도, 연결 사건, 연결 세계 기록, 연결 작전의 날짜를 읽는다. 작전 자체에 날짜가 없으면 사건의 `operation` 연결을 역으로 찾는다. 파일명·제목·본문 속 숫자로 날짜를 추측하지 않는다.
- 날짜가 `DATE LOST`, `AFTER 06:03`처럼 절대연도가 아니면 그 자리에서 미상으로 판정한다. 지하 회수 작전의 연결 세계 기록에 2029년이 있어도 이를 회수 작전의 연도로 가져오지 않는다.
- 미래 표식과 구역은 흐리게 남고 표식은 클릭·탭 대상에서 빠진다. 미래 사건·작전의 목록 행은 제외된다. 열려 있던 미래 사진 판도 닫힌다.
- 연도 미상 표식의 실제 틀과 구역 경계는 점선이다. 지점·작전의 연도 미상 목록은 별도 펼침으로 모았다. 현재 네 세계 구역에는 날짜 필드가 없어 모두 미상이다. 신뢰도 수치로 생성 연도를 추정하지 않았다.
- 세계 지도에도 기존 지점 데이터를 함께 표시하므로 1986·1989·2006·2026·2029·2030년의 사건 표식과 2042년의 관측점을 구분해 볼 수 있다. 지점·좌표를 새로 만들지 않았다.
- 연도 재생은 1975년부터 1.1초에 한 해씩 진행한다. 줄임 모드의 같은 단추는 2042년으로 한 번에 이동한다. 탭이 숨겨지거나 화면을 나가면 재생을 멈춘다.

### 부호 표

우선순위는 `affiliation` → `type` → `tone` → 미확인이다. 표는 `map.js:135`에 있다.

| 데이터 필드 | 값 | 틀 |
|---|---|---|
| `affiliation` | `friendly`, `allied` | 아군 사각형 |
| `affiliation` | `hostile` | 적대 마름모 |
| `affiliation` | `civilian` | 민간 원 |
| `affiliation` | `unknown` | 미확인 네잎 |
| `type` | `friendly`, `hostile`, `civilian` | 각각 아군·적대·민간 |
| `type` | `settlement` | 민간 원 |
| `tone` | `allied`, `hostile` | 각각 아군·적대 |
| 나머지·필드 없음 | — | 미확인 네잎 |

시설·성채·교단이라는 분류만으로 소속을 판정하지 않는다. 현재 데이터에 소속 값이 없는 지점이 많아 미확인 틀이 많다. `secured`도 아군 소속으로 바꾸지 않는다. 현장 판정이 준 `allied`·`hostile`은 기존 단말 사본의 상태이며 조직 계보나 중앙 기록을 바꾸지 않는다. 틀 안에는 시설·성채·사건·관측 같은 한글 두 글자 이하만 넣는다. 기존 작전 재생의 부대 상태 기호는 유지했다.

### 안개·신호

- 상세도 8곳의 신뢰도 7~84%를 직접 읽는다. 안개 불투명도는 `(1 - 신뢰도 / 100) × 0.88`이다. 7%는 0.8184, 84%는 0.1408이다.
- 정적인 SVG `feTurbulence`로 안개를 만든다. 번지는 그림자는 추가하지 않았다. 표식·글자와 신호는 안개 위에 놓인다.
- 관측·검문·성채·감청·계측·감시라는 지점 표제와 독립 동시 관측점에만 짧은 신호선을 붙인다. 지점끼리 잇는 선이 아니다.
- 42~54초마다 보이는 관측점 중 하나가 3.4초 동안 `수신 끊김`으로 표시된 뒤 복구된다. 선택·기록·저장소는 바꾸지 않는다.
- `html[data-silence="on"]`을 `MutationObserver`로 읽어 2042년 열 관측점을 모두 `무응답`으로 표시한다. 상태 해제 시 복구된다. 미상 중계망이나 대륙 사이 통로를 만들지 않았다.
- 줄임 모드에서는 흐름과 임의 중단 타이머를 멈추고 선은 남긴다. 기념일의 실제 무응답 표시는 유지한다.

### 증거 사진·확대

- 표식을 누르면 지도 아래에 작은 판이 열린다. `지점 기록 열기`가 상세 이동을 맡는다. 닫기·Escape는 해당 표식으로 포커스를 돌린다.
- 그림 연결은 직접 `visual`, 권역 그림의 `siteIds`, 지점의 세계 기록 연결을 사용한다. 세계 표식과 상세도의 연결은 기존 `markerDetails` 안에서 같은 이름 또는 동일한 `scenarioStageByItem`일 때만 찾는다. 단지 같은 권역이라는 이유로 그림을 채우지 않는다.
- 그림이 없으면 `연결된 현장 사진 없음`이다. 복원 추정·분석 재구성의 원래 표지를 유지했다.
- 감식 그림은 링크 밖 `.tc-evidence-media` 안에 둔다. `figure`에 매체의 `dtg`, `place`, `coords`, `record` 또는 `assetId`만 전달한다. 사건 날짜를 촬영 시각으로 쓰지 않는다. 현재 연결 그림의 촬영 시각은 없으면 `미상`이다.
- 세계·권역 지도는 1~6배 확대, 휠·두 손가락, 끌기, +/−/0/방향키, `원래대로`를 제공한다. SVG 이미지의 viewBox를 갱신하고 HTML 표식은 36px로 유지한다. 지도별 확대 위치는 현재 탭에서 유지된다.
- `touch-action:none`은 조작 가능한 지도 안에만 적용한다. 끌기는 사진 클릭으로 처리하지 않는다.

## 옛 연출과의 대응

| 옛 파일:줄 | 새 파일:줄 | 대응 |
|---|---|---|
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:562` | `assets/app/js/screens/map.js:5`, `assets/app/js/screens/map.js:482` | 기존 상세 지형을 유지하고 신뢰도 안개를 위에 추가 |
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:612` | `assets/app/js/screens/map.js:482` | 위험·통신·왜곡 표시를 유지하면서 연도 속성으로 나눔 |
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:673` | `assets/app/js/screens/map.js:369` | 지점에 연결된 기존 복원 그림을 작은 판과 감식 표지로 연결 |
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:876` | `assets/app/js/screens/map.js:413`, `assets/app/js/screens/map.js:426` | 세계 지도 기호·범례를 네 가지 소속 틀로 정리 |
| `tools/fixtures/legacy-app/assets/css/map-room.css:609` | `assets/app/js/screens/map.js:274`, `assets/app/css/screens/map.css:37` | 앞 작업 A의 경로 흐름 유지, 확대 시 위치 재계산 |
| `tools/fixtures/legacy-app/assets/css/map-room.css:648`, `:659`, `:685` | `assets/app/js/screens/map.js:322`, `assets/app/css/screens/map.css:32` | 선택 파동·동시 관측·스캔 유지. 새 관측점의 짧은 신호는 `map.css:163` |

연도 막대·신뢰도 안개·사진 작은 판·확대 조작은 A-2의 새 기능이다. 참고 지도 폴더의 `file_00000000eb147206b2f2a8546d8a1118.png`, `file_00000000e160720998cede7db016ca09.png` 두 장을 열어 얇은 선·기하 틀·낮은 명도의 층만 참고했다. 지명·문양·도안을 옮기지 않았다.

## 간략 보기

- 유지한 숨김: 영문 겹표지, 내부 기록 코드, 장식용 패널 표지. 새 연도 미상 펼침의 `UNDATED`도 `.tc-full-only`다.
- 항상 보임: 연도, 지점명, 좌표, 날짜, 상태, 신뢰도, 위험도, 네 가지 틀 범례, 무응답, 사진의 증거 지위·자료 번호·촬영 시각, 확대 상태와 조작 단추.
- 기록 본문과 기존 작전·순례 판정 사실은 고치지 않았다. 상세도와 작전 목록의 연도 미상 자료를 별도 펼침으로 묶었다.

## 검증

- `node tools/stamp-assets.mjs`: 3개 파일 주소 갱신. `index.html`의 나머지 내용은 동일.
- `node tools/verify-app.mjs`: **132/132 통과**. 지도 21개 검사 포함. 기존보다 7개 추가했으며 기존 조건을 약하게 만들지 않았다.
- `node tools/verify-data.mjs`: **356/356 통과**.
- 새 검사는 연도·미상·미래 구역·작전, 재생과 줄임·비활성 정리, 소속 표·범례, 신뢰도별 안개 순서, 드문 중단·열 지점 무응답·감시자 정리, 사진 연결·실제 속성·뒤로 가기, 휠·핀치·드래그·키보드·초기화·리스너 정리를 실행한다.
- DOM 대역의 `append`를 실제 DOM처럼 배열을 자동으로 펼치지 않도록 보정했다. 실제 브라우저에서 발견한 배열의 `[object HTMLElement]` 출력 문제를 수정했고 재발 검사도 넣었다.
- Chrome 153.0.8010.53을 headless로 실행했다. 세계·기본 권역 5곳과 상세도 8곳, 총 13개 화면 × 1280px/375px = **26개 표시 조건**에서 문서 가로 넘침·잘못된 DOM 문자열 없음.
- 데스크톱·모바일 캡처로 지도, 7%·84% 안개, 범례, 작은 사진 판을 육안 확인했다. 허용 파일 범위를 지키기 위해 별도 캡처 파일은 남기지 않았다.
- 실제 브라우저에서 막대의 Home·방향키, 휠, 마우스 끌기, 키보드 확대·초기화·포커스, 사진 열기·상세 이동·뒤로 복귀, 다른 화면을 거친 재진입을 확인했다.
- 모바일 장치 에뮬레이션과 CDP 터치 입력으로 두 손가락 확대를 확인했다. 지도의 viewBox는 줄었고 페이지 `visualViewport.scale`은 1을 유지했다.
- 시스템 움직임 줄이기에서 신호 애니메이션 정지·정적 선 유지·연도 즉시 이동을 확인했다. `data-silence` 전환 때 열 지점의 무응답·복귀도 브라우저에서 확인했다.
- 앱 자바스크립트 오류는 0건이었다. **콘솔 전체 무오류는 아니다**: 기존 Google Fonts 외부 CSS 요청 한 종류가 환경의 `net::ERR_NETWORK_ACCESS_DENIED`로 차단됐다. 이때의 대체 글꼴로 표시 검사를 수행했다.

## 남은 통합 확인과 셸 제안

- 현재 셸의 `PCApp.fx`는 아직 없다. 시스템 설정 경로는 실제 브라우저에서, 셸의 full/reduced 우선권은 선택 API를 넣은 실행 대역에서 확인했다. Claude 셸 병합 뒤 사용자 설정 단추로 다시 확인한다.
- 감식 확대 셸과 기념일을 실제로 설정하는 `pc-anomaly.js`의 통합은 확인하지 못했다. 현재 지도 쪽 표지와 `data-silence` 반응은 검증됐다. 병합 뒤 그림 클릭으로 감식 화면 열기·닫기·초점 복귀를 확인한다.
- 실물 휴대폰·실물 트랙패드, Safari·Firefox, 실제 소리 청취, 원격 배포는 확인하지 않았다. 이번 작업은 새 소리 사건을 추가하지 않았다.
- 외부 폰트 로드가 가능한 환경에서 한국어 글꼴 표시를 다시 확인한다. 현재 셸의 붉은 검정 색·기동·화면 인계·기본 음향 변경은 Claude 담당으로 남아 있다.
- 새 사실 데이터 제안은 채택하지 않았다. 현재 미상인 날짜·소속·촬영 시각을 나중에 보강한다면 출처와 승인된 데이터를 먼저 넣어야 한다.
- 반영 범위는 위 다섯 구현·검사 파일과 이 인계 메모뿐이다. 병합 후 셸의 다른 변경 때문에 주소가 달라지면 stamp와 두 검증을 다시 실행한다.
