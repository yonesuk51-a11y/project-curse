# AGENTS.md — Project Curse 공통 작업 지침

이 저장소에서 일하는 모든 에이전트(Codex, Claude)가 따르는 규칙이다.
세부 기준은 아래 문서가 우선한다. 이 파일은 요약과 협업 절차만 담는다.

| 무엇을 | 기준 문서 |
|---|---|
| 세계관 사실·용어·판정 등급·자료 우선순위 | `WORLD_CANON_LEDGER.md` |
| 화면·색·소리·상호작용 | `PROJECT_CURSE_ART_DIRECTION_GUIDE.md` |
| 기록 본문 문체 | `WRITING_STYLE_GUIDE.md` |
| 이미지·영상·음원 편입 | `ASSET_POLICY.md`, `MEDIA_CREDITS.md` |

## 1. 사이트 정체성

- 2042년 합동작전 단말이다. 현대 전술 체계가 오컬트 현상을 처리하려 하고, 처리하지 못한 것이 공포가 된다.
- 화면은 세 층으로 나뉜다. 셸(현대 전술), 증거(오컬트·옛 매체), 이상(호러)이다. 손상 효과는 증거와 이상 층에만 쓴다.
- 네온·홀로그램 SF, 게임 메뉴(미션 카드·레벨·보상 연출), 점프 스케어를 쓰지 않는다.
- 밀리터리는 장식이 아니라 실제 운용 정보로 드러낸다. DTG, 좌표, 호출부호, 교전 규칙, 판정 태그가 그 정보다.
- 주력 용도는 카카오스토리 자캐 교류다. 참가자가 읽어야 할 정보는 연출로 가리지 않는다.
- 세부 기준은 `PROJECT_CURSE_ART_DIRECTION_GUIDE.md`가 정한다.

## 2. 세계관 규칙

- 자료 우선순위는 `WORLD_CANON_LEDGER.md`를 따른다. 낮은 순위 자료가 높은 순위 자료를 덮어쓰지 않는다.
- 보호 원문 `Cults_871104`, `Immortality_860201`은 수정하지 않는다. `index.html` 인라인 기록, `docs/` 독립 문서, `main.js` 시네마틱 대본이 해시로 잠겨 있다.
- 확인되지 않은 것을 확정문으로 쓰지 않는다. 기록끼리 충돌하면 지우거나 합치지 말고 `상충 기록` 또는 `결정 대기`로 남긴다.
- 리버스(사건), 괴이(비인간 존재), 타락자(변질된 인간), 능력자(변칙을 쓰는 인간)를 동의어로 쓰지 않는다.
- 능력에는 반드시 대가가 있고, 대가는 대부분 지각·기억·신원으로 지불된다. 능력을 많이 쓴 사람일수록 관측 자격을 잃는다.
- 실제 전쟁·참사·역병의 원인을 초자연 현상으로 바꾸지 않는다. 실제 피해자를 가공 장치로 쓰지 않는다.
- 새 기록에는 작성자·수신자·목적·정보 한계를 정한다. 관찰과 해석을 한 문장에 넣지 않는다.

## 3. 코드 규칙

- 사실 데이터는 `assets/js/data/`, 화면 로직은 `assets/js/pages/`에 둔다.
- `assets/js/main.js`에는 이미 패치 블록이 81개 쌓여 있다. 새 기능을 여기에 덧붙이지 않는다.
- 화면 이동은 `ProjectCurseShell.navigate()`를 쓴다. 이미 5개인 `routeTo` 함수를 새로 만들지 않는다.
- 없는 키를 다른 값으로 조용히 바꾸는 폴백을 쓰지 않는다. 예를 들어 없는 세력을 U.A.C 문서로 열면 안 된다. 없으면 없다고 표시한다.
- 영문→한글 용어 치환은 토큰 단위로만 한다. 부분 문자열로 바꾸면 `CLASSIFICATION` 같은 단어가 깨진다.
- 이벤트 리스너와 타이머는 화면을 떠날 때 정리한다. 같은 화면에 두 번 들어와도 중복으로 붙지 않게 한다.

### 화면별 담당 모듈

| 화면 | 모듈 |
|---|---|
| 셸·탐색 | `assets/js/core/app-shell.js` (`ProjectCurseShell`) |
| 화면 전환 | `assets/js/core/transition-controller.js` |
| 화면 머리글 | `assets/js/core/channel-identity.js` |
| 단말 상태(홈) | `index.html` 마크업 + `assets/js/pages/terminal-home.js` |
| 상황 관제 | `assets/js/pages/map-room.js` |
| 세계 기록 | `assets/js/pages/world-history.js` |
| 세력 분석 | `assets/js/pages/faction-analysis.js` |
| 기록보관소 | `assets/js/pages/archive-consolidation.js`, 문서 뷰어 `archive-document.js` |
| 인물 기록 | `assets/js/pages/personnel-archive.js` |
| 순례 시나리오 | `assets/js/pages/pilgrimage-scenario.js` |

## 4. 검증 — 커밋 전에 반드시

- `node tools/verify-package.mjs`가 전부 통과해야 한다.
- 검증 조건을 약하게 바꿔서 통과시키지 않는다. 기록이나 세력을 추가해 개수가 바뀐 경우에만 개수 단언을 새 값으로 고치고, 그 이유를 커밋 메시지에 적는다.
- 화면을 바꿨으면 브라우저에서 아래를 확인한다. 화면 전환 애니메이션이 있어서 전환 결과 확인까지 3~6초가 걸린다.
  - 데스크톱(1280px)과 모바일(375px) 표시, 가로 넘침 없음
  - 바꾼 화면으로 들어갔다가 다른 화면으로 나오는 왕복 이동
  - 키보드 탭 이동과 포커스 표시
  - `prefers-reduced-motion` 설정에서 전환·스캔 효과가 꺼지는지
  - 콘솔 오류 0

## 5. 이미지 생성

- **새로 만드는 그림**과 **기존 그림 수정**을 구분한다. 새 그림은 구도·카메라·포즈·체형을 생성하는 쪽이 정하고 이전 구도를 재사용하지 않는다. 기존 그림을 수정할 때는 재설계 요청이 없으면 구도·포즈·인물 정체성·체형·의도된 변형을 보존한다.
- 화풍 기준은 `참고용/지옥` 원본이 아니라 프로젝트가 만든 결과물이다. `output/imagegen/`의 기준 파일은 다음과 같다.
  - `20260912-nhc-group-hell-redraw-v4/nhc-group-hell-redrawn-v4.png` — 군상 기본
  - `20260912-six-hell-remakes-v1/04-023-tactical-trio-remake.png` — 전술 장비
  - `20260912-six-hell-remakes-v1/03-133-shelter-survivors-remake.png` — 체형이 제각각인 군상
  - `20260912-six-hell-remakes-v1/02-131-shoulder-buddy-pair-remake.png` — 두 인물 근접, 얼굴
  - `20260918-multi-eye-swordswoman-body-horror-remake-v1/` — 바디호러
  - `20260918-hooded-mutant-dual-daggers-hell-remake-v1/`의 `-v2-no-logo.png` — 어두운 단일 인물
- 화풍은 얇고 고른 마감선, 무채도 올리브·카키·회녹·탁한 갈색의 플랫 매트 채색이다. 그림자는 경계가 딱 떨어지는 덩어리로 칠한다. 얼굴에는 짧아도 코 획이 보이고, 머리카락은 덩어리로 처리한다. 배경은 인물과 같은 밀도로 단순하게 그린다. 회색 스케치 마감이 아니다.
- 빛은 렌더링하지 말고 그린다. 빛은 경계가 딱 떨어지는 평평한 도형이다. 글로우, 광선, 연무, 블룸, 바닥 반사, 피사계심도, 공기원근을 넣지 않는다. 화면은 거의 검정·짙은 회색·중간 회색·밝은 값의 네 가지 평평한 값으로 구성한다.
- 전술 장비는 2040년대지만 현실에 발을 딛는다. 플레이트 캐리어, 헬멧과 야시경 마운트, 무전기, 슬링, 방독면, 봉인 운반함 정도다. SF 외골격이나 발광 장비는 그리지 않는다.
- 배경은 맡기지 않는다. `폐허 석조 + 잿빛 평야 + 흐린 회색 하늘` 조합을 피하고, 장소는 대흑림 성채, 서부 순례 회랑, 데드존 검문소, 지하문, 도시 폐쇄시설, 북해권·남방 해안에서 고른다.
- 바디호러와 고어는 실제로 그린다. 훼손은 구체적으로 묘사하되, 렌더링은 경계가 딱 떨어지는 어두운 갈적색 덩어리로 평평하게 처리한다. 아이와 성적 요소는 넣지 않는다.
- 읽을 수 있는 글자, 실존 부대 마크, 지어낸 부대 휘장을 넣지 않는다.
- 노이즈·스캔선·가림 같은 손상 효과를 그림 파일에 굽지 않는다. 필요하면 사이트 표시층에서 입힌다.
- 결과물은 `output/imagegen/<YYYYMMDD>-<slug>/`에 저장한다. 채택되기 전에는 `assets/`에 넣지 않고, 채택된 것만 `ASSET_POLICY.md` 절차로 편입한다.
- `참고용/지옥` 원본의 인물·문양·장비·구도를 옮겨오지 않는다.

## 6. 협업 절차 (Claude ↔ Codex)

역할은 다음과 같이 나눈다.

| 결정 | 누가 |
|---|---|
| 이미지 채택 | 사용자 |
| 정사 확정(`WORLD_CANON_LEDGER.md` 등록) | 사용자 |
| 정사 정합성·검증·병합 | Claude |
| 이미지 생성, 분리된 기능 구현 | Codex |

- 코드 검토를 통과했다고 이미지가 채택된 것은 아니다. 그림에 새로 그려진 설정도 정사로 확정된 것이 아니다.
- 한 작업에서 한 파일은 한 에이전트만 고친다.
- Codex는 `codex/<주제>` 브랜치 또는 `output/` 폴더에서 작업한다. main에 직접 커밋하거나 푸시하지 않는다.
- 작업이 끝나면 인계 메모를 남긴다. 무엇을 바꿨는지, 무엇을 확인했는지, 확인하지 못한 것, 파일 경로와 버전, 사이트에 반영할 범위를 적는다.
- 저장소 밖의 파일을 수정하지 않는다. `reset --hard`, `push --force`, 브랜치 삭제 같은 파괴적인 git 명령을 쓰지 않는다.
