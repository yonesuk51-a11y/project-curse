# MapPatch 5.35.0 — Archive Signal Library

Released: 2026-08-20

## 기록보관소 개편

- 공개 기록 13개를 단순 영상·문서 행 목록에서 카드형 신호 라이브러리로 재구성했다.
- 사건·회수, 권역, 규정·안내, 작전, 개체, 교단·오염 분류와 영상 전용 필터를 추가했다.
- 기록 ID, 제목, 설명, 분류와 관련 표식을 함께 찾는 통합 검색을 추가했다.
- 공개 기록 수, 영상 시퀀스 수, 복원 추정본 수와 출처 대조 대기 수를 동적으로 계산한다.
- 기존 조건부 현장 판정 기록과 범위별 초기화 기능은 새 라이브러리 아래에 그대로 유지했다.

## 출처와 이미지 상태

- 각 카드에 위험도, 날짜, 열람 형식과 이미지 출처 상태를 표시한다.
- Archive ENEX 원본 계열 이미지는 `원본 보존`으로 표시한다.
- 대흑림·데드존 재구성 이미지는 `복원 추정본`으로 표시한다.
- 원본 계보가 아직 확인되지 않은 기존 자산은 `출처 대조 대기`로 표시한다.
- 확인 가능한 이미지가 없는 기록은 임의 이미지를 배정하지 않고 코드 기반 신호판을 사용한다.
- 출처 상태 세 가지의 의미를 색인 하단에서 다시 설명한다.

## 상호작용과 화면 적응

- 기록보관소 진입 시 카드가 중요도 순으로 차례로 조립되는 연출을 추가했다.
- 카드 호버·포커스·장착 응답과 영상·문서별 행동 표식을 추가했다.
- 분류 전환에는 낮은 음량의 전용 인터페이스 효과음을 연결했다.
- 문서 장착음과 영상 재생음은 기존 런타임이 계속 단독으로 소유해 중복 재생을 막았다.
- 태블릿은 2열, 휴대전화는 이미지 신호판과 본문이 나란히 놓이는 1열 기록 카드로 전환한다.
- `prefers-reduced-motion` 환경에서는 카드 진입과 장착 애니메이션을 제거한다.

## 구조와 검증

- `archive-registry.js`가 분류, 위험도, 날짜, 출처 상태, 표지와 검색 표식을 소유한다.
- `archive-consolidation.js`가 카드 생성, 필터, 검색, 결과 안내와 기존 판정 기록을 소유한다.
- `archive-consolidation.css`가 보관소 화면의 데스크톱·태블릿·모바일 정체성을 소유한다.
- 런타임 스키마를 `project-curse-v26`으로 올렸다.
- 정적 패키지와 내부 문서 검증에 분류 메타데이터, 출처 정직성, 필터·검색·음향 사건 검사를 추가했다.
