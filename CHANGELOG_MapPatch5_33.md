# Map Patch 5.33 — Readable Settings & Session Health

## 목표

기술 계측을 모두 펼쳐 놓던 설정 패널을 일반 사용자 중심으로 다시 구성하고, 문제 해결에 필요한 원자료는 접이식 고급 진단으로 분리했다.

## 변경 사항

- 설정 상단에 현재 적용 상태 요약을 추가했다.
  - 자동 최적화됨
  - 균형 모드 적용됨
  - 데이터 절약 중·고정
  - 고화질 고정
  - 오프라인 열람 중
- 연결, 미디어 준비, 환경음 상태를 세 개의 짧은 상태표로 표시한다.
- 설정을 `화면·전송`과 `음향` 두 구역으로 재배치했다.
- 품질과 성능 원자료를 `고급 세션 진단` 안으로 접었다.
- 진단을 열면 연결·지연·메모리·CPU, 부팅·DOM·전송·전환·세션·힙을 확인할 수 있다.
- 측정값을 다시 읽는 로컬 새로고침 조작을 추가했다.
- 세션의 표시 시간과 백그라운드 체류 시간을 별도로 계측한다.
- Chromium 계열에서는 JavaScript 힙 사용량을 표시하고, 미지원 브라우저에서는 `N/A`로 안전하게 대체한다.
- 상단 설정 버튼과 모바일 설정 항목에 현재 품질 상태를 함께 표시한다.
- 모바일 설정은 상단 닫기·하단 완료 조작을 고정하고 본문만 독립 스크롤한다.
- 숨겨진 연결 복구 패널이 투명한 클릭 영역으로 남던 문제를 수정했다.
- 스키마를 `project-curse-v24`로 올렸다.

## 검증

- 전체 JavaScript 구문 검사 통과
- 패키지 검사 `380/380`
- 내부 셸 검사 `183/183`
- 보호 인라인·독립 문서 해시 4개 유지
- 데스크톱에서 상태 요약, DATA SAVE, 고급 진단, 오프라인 전환과 포커스 복귀 확인
- 모바일 390×844에서 본문 독립 스크롤, 고정 완료 버튼, 가로 넘침 0 확인
- 숨겨진 복구 레이어 `display:none`, 포인터 가로채기 없음 확인
- 브라우저 오류 0

## 빌드

- 버전: `5.33.0`
- 코드명: `Readable Settings & Session Health`
- 스키마: `project-curse-v24`
