# Project Curse 5.46.0 — Field Panel Control

## 목표

모바일 지도에서 정보 패널이 지도보다 먼저 화면을 점유하는 문제를 줄이고, 세계 기록의 위치 버튼이 실제로 선택할 수 없는 좌표로 사용자를 보내지 않도록 연결 증거 기준을 강화한다.

## 모바일 FIELD INTELLIGENCE 패널

- 지역 상황도, 세부 권역, 작전지도에 같은 접기·펼치기 인터페이스를 적용했다.
- 권역·구역 개요는 접힌 상태로 시작한다.
- 접힌 패널은 현재 선택 이름과 펼침 버튼만 약 60px 높이로 남긴다.
- 지역 표식, 2042 동시관측점, 세부 현장, 연결 경로의 다음 지점 또는 새 작전 단계를 선택하면 패널이 자동으로 열린다.
- 지도 모드나 권역을 바꾸면 새 개요를 먼저 볼 수 있도록 다시 접는다.
- 수동 펼침 상태는 `project_curse_map_session_v1`에 저장하고 현재 탭에서 새로고침할 때 복원한다.
- 데스크톱에서는 접기 버튼을 표시하지 않고 기존 3열·2열 전체 정보 배치를 유지한다.

## 접근성

- 접기 버튼은 `aria-expanded`로 현재 상태를 알린다.
- `aria-controls`는 각 지역·세부·작전 정보 본문의 실제 ID와 연결된다.
- 버튼 이름은 `지도 선택 정보 펼치기`와 `지도 선택 정보 접기`로 상태에 맞게 바뀐다.
- 키보드 포커스 윤곽과 충분한 터치 높이를 제공한다.

## 지도 연결 증거 게이트

- 세계 기록은 `ProjectCurseMapRoom.markers[].incident`를 기준으로 실제 표식이 있는 사건을 판독한다.
- 등록부에 위·경도가 있더라도 렌더링 표식이 없으면 위치 버튼을 만들지 않는다.
- `1975 아마리온 설립`의 빈 위치 연결을 제거했다. 아마리온·F.H.C 세력 분석과 연결 기록은 유지한다.
- 도쿄 지부 기록 등 승인된 표식이 있는 사건은 위치 버튼을 유지하며, 이동 후 해당 권역과 표식을 선택하고 모바일 정보 패널을 자동으로 연다.
- 작전 버튼도 현재 지도 작전 목록에 실제 ID가 있을 때만 생성한다.

## 음향과 정사 경계

- 새 음원과 `/assets/audio/core/` 요청을 추가하지 않았다.
- 패널 토글은 기존 낮은 음량의 `contact` 의미 효과음만 재사용한다.
- 지도 표식의 부재는 사건 자체가 허구이거나 삭제됐다는 뜻이 아니다.
- 패널 접힘과 세션 복원은 공통 세계사·작전 판정·순례 결과를 수정하지 않는다.

## 검증

- 아마리온 위치 링크 비노출과 도쿄 실제 표식 연결 비교
- 데스크톱에서 패널 본문 상시 표시 및 접기 버튼 비노출
- 모바일 개요 접힘, 수동 펼침, 새로고침 복원
- 지역 표식·세부 현장·작전 단계 자동 펼침
- 390×844 및 1440×1000 가로 넘침, 콘솔 오류와 신규 핵심 음향 요청 검사
