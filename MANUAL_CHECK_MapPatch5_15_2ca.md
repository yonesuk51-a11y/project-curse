# Manual Check — 5.15.2ca

## 자동 검사

1. 브라우저 강력 새로고침 후 `ProjectCurseQA.patch`가 `5.15.2ca`인지 확인한다.
2. `ProjectCurseQA.checkEquipmentArchiveUI()` 실행:
   - `ok: true`
   - `cardCount: 6`
   - `openCount: 6`
   - `axisCount: 6`
   - `detailCount: 6`
   - `relatedActionCount: 18`
   - `heightSpread: 0` 또는 허용 범위 이내
3. `ProjectCurseQA.screenSweep()` 실행:
   - errors 0
   - warnings 0
   - info 0

## 기록보관실 화면

1. `장비·봉쇄 기록` 그룹이 별도 기록면으로 표시되는지 확인한다.
2. 다음 카드 6개가 3×2 배열로 표시되고 `기밀 처리됨` 문구가 없는지 확인한다.
   - 오염 차폐 장비군
   - 신호 억제 장비군
   - 혈성 오염 대응 장비군
   - 회수·소각 장비군
   - 봉쇄 인프라 체계
   - 현장 제압 장비군
3. 장비 카드 5개는 `장비 기록`, 봉쇄 인프라 카드는 `봉쇄 인프라`로 표시되는지 확인한다.
4. 검색창에서 `혈성 오염`을 검색했을 때 해당 카드만 남는지 확인한다.
5. `장비 기록`, `봉쇄 인프라` 필터와 `전체` 초기화가 정상 동작하는지 확인한다.

## 상세 기록과 연결

1. 카드 6개의 `기록 열람` 버튼이 모두 작동하는지 확인한다.
2. 각 상세 기록에 다음 5개 구획이 존재하는지 확인한다.
   - 포함 항목
   - 운용 원칙
   - 성능 저하·사용 중지·철수 기준
   - 회수·폐기 절차
   - 현장 실패 기록
3. 연결 장비 기록을 연속으로 열어도 상세 화면이 자동으로 닫히지 않는지 확인한다.
4. `세력 파일` 버튼이 해당 세력 페이지로 이동하는지 확인한다.
5. `지역 상황도` 버튼이 대응 마커를 선택하는지 확인한다.
6. `기록 LIST로 복귀`가 카드 목록으로 정상 복귀하는지 확인한다.
