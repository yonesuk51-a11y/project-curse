# MANUAL CHECK — MapPatch 5.15.2ax

## 1. 재진입
1. 기록 보관소에서 `종교 / Cults_871104`를 연다.
2. 중간 페이지에서 `돌아가기`를 누른다.
3. 같은 기록을 다시 연다.
4. 인트로부터 정상적으로 다시 시작되는지 확인한다.
5. 끝까지 본 뒤 다시 열어도 동일하게 초기 시작되는지 확인한다.

## 2. 레이아웃
- `타락교`, `혈교`만 좌측 설명 / 우측 이미지로 보이는지 확인.
- 나머지 사진 기록은 중앙 큰 이미지, 이미지 아래 캡션, 그 아래 보고문 순서인지 확인.
- `혈액 이동 경로 조정`, `혈무의 제작 과정`도 중앙 증거사진형으로 바뀌었는지 확인.

## 3. 표시 방식
- 종교 시퀀스 설명이 한 줄씩 나오지 않고 이미지/캡션/설명 블록이 한 번에 표시되는지 확인.
- `요약:` 또는 `특징:` 라벨이 시퀀스와 정적 문서에서 보이지 않는지 확인.

## 4. 타락의 과정 및 형태
- `타락의 과정 및 형태`는 기존 이미지가 보이는지 확인.
- `IMAGE0923 / SIGNS OF CORRUPTION`은 별도 `타락 징후` 페이지로 보이는지 확인.
