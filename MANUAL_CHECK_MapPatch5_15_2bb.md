# MANUAL CHECK 5.15.2bb

1. Open `종교` and check `신원불명의 신자`. The evidence card should be centered in the full PC viewport.
2. Check `타락교` and `혈교`. Only these keep left text + right image.
3. Check `융합성 타락`. First click mutates the explanation; the next click advances.
4. Open `불멸을 향해`. The opening keeps briefing text + right image; IMG time pages are centered.
5. On mobile width, pages should stack vertically and allow scroll without center-cropping.
