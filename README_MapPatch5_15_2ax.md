# MapPatch 5.15.2ax — EvidenceCenter_BlockRevealPass

## 목적
5.15.2aw에서 남아 있던 종교 시퀀스의 배치/표시/재진입 문제를 수정했다.

## 핵심 변경
- `타락교`, `혈교`만 설명형 좌문장/우이미지 레이아웃 유지.
- 그 외 사진 기록은 중앙 증거사진형으로 변경.
  - 큰 이미지
  - 이미지 아래 `figcaption` 메타데이터
  - 그 아래 짧은 현장 보고문
- 이미지 아래 빈 공간을 캡션 영역으로 사용하도록 구조 변경.
- `요약:` / `특징:` 같은 노골적 라벨 제거.
- 설명문을 짧은 현장 보고문 톤으로 재작성.
- 종교 시퀀스는 한 줄씩 나오지 않고 이미지/캡션/설명 블록이 한 번에 표시되도록 변경.
- `돌아가기` 후 같은 기록 재열람, 끝까지 본 뒤 같은 기록 재열람 시 꼬이는 문제를 막기 위해 시퀀스 런타임 강제 초기화 추가.
- `타락의 과정 및 형태`는 기존 이미지를 복구하고, `IMAGE0923 / SIGNS OF CORRUPTION`은 별도 `타락 징후` 항목으로 분리.

## 수정 범위
- `index.html`
- `docs/Cults_871104/index.html`
- `assets/js/main.js`
- `assets/css/style.css`

## 보존 범위
- `Immortality_860201` 본문/순서/구조는 변경하지 않음.
- 지도, 세력 관계도, 오디오 파일, 메뉴 구조는 변경하지 않음.
