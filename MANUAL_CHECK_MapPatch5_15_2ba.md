# Manual Check — 5.15.2ba

1. Open `종교`. On central image pages, confirm the image is followed by `IMG-#### + page title`, then explanation.
2. Open `융합성 타락`. Confirm the normal explanation appears first, then click once and confirm only the explanation/title metadata mutates. A second click should advance.
3. Open `불멸을 향해`. Confirm the first briefing page no longer shows large Korean/English title/subtitle.
4. On photo time pages such as `16:31`, confirm the big time title is gone and `IMG001 / 16:31` appears under the image.
5. Check mobile width: images should use contain and stack vertically, not crop to the middle.
6. Return and reopen both records to confirm reset.
