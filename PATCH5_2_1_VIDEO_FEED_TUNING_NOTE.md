# PATCH 5.2.1 — Video Feed Layout Tuning

- 영상 기록 이미지 주변의 `FRAME DROP` / `SIGNAL TEAR` 표시가 figure 박스 폭을 넓혀 빈 공간처럼 보이는 문제를 정리했습니다.
- 프레임 상태 문구를 이미지 위 오버레이로 고정해 본문 레이아웃 폭을 차지하지 않도록 조정했습니다.
- `FRAME DROP`은 `프레임 누락`, `SIGNAL TEAR`는 `신호 손상`으로 병기했습니다.
- 5.2 영상 기록 효과, 5.1.1 F.H.C 검열 효과, 5차 Safe UI, 4.9 기능 복구 기반은 유지합니다.
- 지도 / 세력 / 배경음 / 기록 열람 기본 함수는 건드리지 않았습니다.
