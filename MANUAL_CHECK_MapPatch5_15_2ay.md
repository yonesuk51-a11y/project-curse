# Manual Check — 5.15.2ay

1. Open `종교`.
2. Confirm no `SIGNS OF CORRUPTION` full caption screenshot appears.
3. Confirm no separate `변조 경고` page appears.
4. Confirm `타락의 과정 및 형태` uses the original project image and later shows the red autocannibalism warning.
5. Confirm `침묵성 타락` and `융합성 타락` use the two approved clean images only.
6. Confirm evidence pages show `IMG-#### + title` under the image, with description below.
7. Finish and reopen the record to verify the sequence resets cleanly.
