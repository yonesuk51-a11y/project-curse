# Project Curse 5.44.0 — Signal Concordance Map

## 결과

2042년 `삼야 무응답`을 세계 기록의 문장에만 머물지 않고 상황 관제도에서 직접 판독할 수 있는 독립 신호층으로 구현했다.

## 지도 데이터

- 대흑림 성채 종 장부 6개 지점 `G01–G06`
- 데드 존 검문소 무응답 기록 4개 지점 `D01–D04`
- 사건 코드 `SYNC-2042-1031`, 관측 시간 `61H 01M`
- 지점별 복구 호출 부호와 장부 요약
- 세계 기록 `2042-10-31-three-night-silence` 직접 연결
- `connection: UNRESOLVED`, `route: null` 데이터 경계

## 화면 동작

- 세계 보기에서 열 개 관측점 동시 표시
- 남미 대흑림에서 성채 여섯 곳, 북미 데드 존에서 검문소 네 곳만 표시
- 표식 선택 시 사건·현장·복구 호출·장부 상태를 우측 판독 패널에 표시
- 레이어를 끄면 관측점과 선택 상태를 함께 제거
- 런타임 `showSynchrony(eventId, pointId)` 직접 진입 지원
- 모바일에 전체 지도 레이어용 가로형 빠른 제어막대 추가

## 정사 보호

관측점 사이에는 선, 경로 또는 네트워크 도형을 생성하지 않는다. 화면과 정사 대장 모두 `NO ROUTE / NO GEOGRAPHIC LINK`를 명시하며, 동시 발생을 두 대륙의 지리적 연결로 해석하지 않는다.

## 음향

새 음원은 추가하지 않았다. 지도 조작은 기존 의미 효과음만 호출하며 5.42.0 음향 기준을 유지한다.

## 검증 범위

- 패키지 정적 무결성
- 보호 기록 4개 해시
- 동시관측점 10개와 권역별 6/4 구성
- 연결 경로 부재와 정사 경계 문구
- 데스크톱·모바일 레이어 토글, 지점 선택, 연표 이동, 화면 넘침과 콘솔 오류
- `/assets/audio/core/` 요청 부재
