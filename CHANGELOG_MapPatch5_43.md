# Project Curse 5.43.0 — Core Sound Identity

## 자체 생성 핵심 음향

- 단말 연결·분리, 메뉴 열기·닫기·선택·뒤로, 접근 거부, 기록 마운트·언마운트, 증거 프레임, 통신 연결과 작전 확인의 12개 신호를 추가했다.
- `tools/build-core-sounds.mjs`가 외부 음원·샘플 없이 48 kHz, 16-bit, 모노 PCM WAV를 결정론적으로 합성한다.
- 생성기를 인자 없이 실행하면 저장된 12개 파일과의 바이트 일치를 검사하고, `--write`일 때만 전체 팩을 다시 쓴다.
- 초기 부팅의 연결음은 새 단말 연결 신호를 사용한다. 기존 기록 영상·기록글 배경음과 보호 기록 전용 음향은 변경하지 않았다.

## 의미 이벤트와 음향 정체성

- 공통 UI 의미 이벤트는 새 핵심 음향 ID를 우선 사용하고 기존 파일은 호환용 대체 경로로 남긴다.
- 다섯 주 화면에는 `CRT RELAY`, `GRID PULSE`, `TAPE INDEX`, `SIGNAL INTERCEPT`, `VAULT DECK` 프로필을 적용했다.
- 내부 문서, 대흑림, 데드존, 현장 안내, 시나리오와 회수 시나리오까지 총 11개 프로필이 버스 게인과 미세 재생 속도를 조절한다.
- 메뉴 뒤로와 기록 언마운트 사건을 분리하고, 문서 닫기에서 기록 언마운트 신호를 재생한다.
- 경고 우선순위, 같은 버스의 동시 재생 제한, 환경음 덕킹과 빠른 중복 입력 쿨다운을 공통 재생기에서 유지한다.

## 설정 음향 실험실

- 표시·음향 설정에 접이식 `CORE SOUND IDENTITY / PC-CORE-01` 패널을 추가했다.
- 12개 신호는 한국어 이름, 계열과 밀리초 길이를 확인하며 개별 미리듣기할 수 있다.
- MASTER, AMBIENT, INTERFACE, RECORD, ALERT 다섯 버스의 음량을 슬라이더로 조절하고 브라우저에 저장한다.
- 현재 화면의 음향 프로필, 핵심 신호 수, 마지막 재생 신호와 음소거 상태를 표시한다.
- 모바일에서는 믹서와 신호 카드를 두 열로 재배치하고, 키보드 포커스와 모션 감소 설정을 유지한다.

## 출처와 공개 감사

- 공개 미디어 대장은 186개로 갱신됐다: 이미지 144개, 음원 35개, 영상 7개다.
- 새 12개 음원은 `PROJECT_SYNTHESIS / PROJECT_GENERATED`로 등록했다.
- 기존 음원 23개와 영상 7개는 계속 `LICENSE_REVIEW`이며 자체 생성 팩의 상태를 승계하지 않는다.
- 프로젝트 관리 자산은 36개, 출처·권리 검토 대상은 기존과 같은 150개다.
- 기록보관소 감사 UI는 음원 총수와 자체 생성·검토 대기 수를 따로 표시한다.

## 버전과 보호 범위

- 빌드를 `5.43.0 Core Sound Identity`, 스키마를 `project-curse-v36`으로 갱신했다.
- `Cults_871104`, `Immortality_860201` 인라인·독립 기록의 보호 해시는 변경하지 않았다.

## 검증

- JavaScript 문법, 생성기 재현성, WAV 헤더·채널·샘플레이트·비트 깊이, 미디어 대장 재생성 일치를 검사한다.
- 패키지 검증은 12개 핵심 음향의 파일 존재, 의미 이벤트 전부의 유효한 음향 연결, 11개 프로필과 설정 미리듣기·믹서 구조를 검사한다.
- 브라우저에서는 설정 패널, 미리듣기, 볼륨 저장, 채널별 프로필, 메뉴 의미음, 데스크톱·모바일 가로 넘침과 콘솔 오류를 확인한다.
- 패키지 검증 `487/487`, 내부 문서 셸 검증 `193/193`, 로컬 Chrome 음향·반응형 검증 `20/20`을 통과했다.
