# MapPatch 5.15.2bb — StageTemplate_ResponsiveHubFix

- Sequence stage templates are separated by layout type.
- Evidence/photo pages no longer inherit the left column from briefing pages.
- PC uses a full viewport stage with centered evidence/photo templates.
- Mobile uses a vertical stack with contain-style media.
- Side-menu shell keeps a hub-sized PC rail and mobile stack/drawer compatibility.
