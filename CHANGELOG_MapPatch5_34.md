# Map Patch 5.34 — Cinematic Channel Handoff

## 목표

메뉴 선택 뒤 화면이 즉시 교체되는 느낌을 줄이고, 다섯 채널이 각자 다른 폐쇄 단말 시스템으로 연결되는 감각을 완성한다.

## 변경 사항

- 채널 전환을 제어권 해제, 채널 획득, 화면 조립의 세 단계로 분리했다.
- 단말, 관제, 연대기, 정보 분석, 기록보관소에 서로 다른 전환 문양과 신호 명칭을 적용했다.
- 퇴장, 전환 차단, 미디어 준비, 정착 대기, 진입 시간을 별도로 제어한다.
- 새 화면은 채널 표식, 제목, 핵심 계기, 세부 카드 순서로 등장한다.
- 빠른 연속 선택에서는 마지막 목적지만 이어 받고 동일 목적지 요청은 하나로 합친다.
- 전환 중 콘텐츠 영역에 접근성 `aria-busy` 상태와 보류 채널 표식을 제공한다.
- 첫 기동은 약 9.5초, 세션 복원은 약 5.9초, 기록 복귀는 약 5초로 조정했다.
- 로딩에 LINK, INDEX, VERIFY, ACCESS 관문과 최종 접근 승인 유지 구간을 추가했다.
- 메뉴 열기, 닫기, 선택음의 음색·음량·재생 간격을 분리했다.
- 채널 요청음은 최종 채널 도착음보다 낮게 조정해 중첩감을 줄였다.
- 모션 감소 환경에서는 문양 회전과 점멸을 제거하고 짧은 상태 전환만 유지한다.

## 검증

- 변경 JavaScript와 검증 스크립트 구문 검사 통과
- 패키지 검사 `380/380`
- 내부 문서 셸 검사 `183/183`
- 보호 인라인·독립 문서 해시 4개 유지
- Chrome 콜드 부팅 실측 약 `9.68초`, 진행률 100%, 접근 관문 4개 완료 확인
- 데스크톱 `1440×1000`에서 전환 중 단계·접근성 busy 상태와 종료 후 단일 활성 화면 확인
- 모바일 `390×844`에서 빠른 메뉴의 순차 등장, 좌우 안전 여백과 가로 넘침 0 확인
- 브라우저 오류 0

## 빌드

- 버전: `5.34.0`
- 코드명: `Cinematic Channel Handoff`
- 스키마: `project-curse-v25`
