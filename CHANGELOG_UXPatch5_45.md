# Project Curse 5.45.0 — Chronology Handoff

## 목표

화면에 들어올 때 채널의 성격은 분명히 전달하되, 실제 열람을 시작한 뒤에는 상단 소개가 본문을 밀어내지 않게 한다. 또한 2042년 `삼야 무응답`의 연표와 관측도를 어느 방향에서도 오갈 수 있게 하면서 두 대륙 사이에 지리적 연결이 있다는 오해는 만들지 않는다.

## 채널 밀도 전환

- 단말 홈을 제외한 상황 관제·세계 기록·정보 분석·기록보관소는 첫 진입 후 1.8초 동안 전체 채널 소개를 보여준다.
- 이후 설명문과 상징 문양을 접고 제목·번호·핵심 계기만 남긴다.
- 900px 이하에서는 계기도 접어 제목과 작업 본문에 우선권을 준다.
- 접기·펼치기 버튼은 `aria-expanded`와 실제 상태를 함께 갱신한다.
- 사용자가 선택한 채널별 밀도는 `project_curse_channel_density_v1` 세션 저장소에만 기록한다. 새 탭에서는 다시 기본 소개 흐름으로 시작한다.
- `prefers-reduced-motion` 환경에서는 지연 연출 없이 바로 압축한다.

## 연표 ↔ 관측도 인계

- 세계 기록 상세는 `ProjectCurseMapRoom.synchronyEvents[].history`를 읽어 연결 가능한 동시관측 사건을 자동으로 찾는다.
- `삼야 무응답 관측도 · 10개 신호`를 누르면 상황 관제의 세계 보기에서 열 개 독립 표식을 함께 연다.
- 기존 지도 표식의 `세계 기록에서 삼야 무응답 열기`와 결합해 양방향 이동을 완성한다.
- 역방향 진입은 특정 표식 하나를 임의 선택하지 않는다. 사용자가 세계 보기에서 두 권역을 먼저 비교하도록 한다.

## 지도 세션 복원

`project_curse_map_session_v1`은 현재 탭 안에서 다음 항목만 보존한다.

- 지역·세부·작전 모드
- 선택 권역과 독립 관측 표식
- 세부 지점·작전 단계
- 세계 지도와 세부 지도의 레이어 표시 상태

이 값은 공통 정사, 작전 판정, 순례 결말 또는 판정 보관소를 수정하지 않는다.

## 음향 및 정사 경계

- 새 음원과 `/assets/audio/core/` 자산을 추가하지 않았다.
- 채널 밀도 버튼은 기존 `contact` 의미 효과음을 낮은 음량으로 재사용한다.
- `NO ROUTE / NO GEOGRAPHIC LINK` 경계를 유지한다.
- 화면 이동과 세션 복원은 통로·항로·시간 중첩의 증거가 아니다.

## 검증

- 패키지·보호 문서 해시·내부 문서 셸 회귀검사
- 2031–2042 연표 데스크톱·모바일 검사
- 2042 독립 관측도 데스크톱·모바일 검사
- 채널 첫 진입, 자동 압축, 수동 펼침 보존, 연표→지도 인계와 지도 새로고침 복원 전용 검사
- 1440×1000 및 390×844에서 가로 넘침과 콘솔 오류 검사
