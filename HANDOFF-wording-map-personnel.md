# 작전 지도·인물 기록 문구 정리 인계

- 작업일: 2026-09-25
- 작업: A-3, 브랜치 `codex/wording-map-personnel`
- 기준 커밋: `7915a8e33c38ddab0618e55c0b07db25622bd53b`
- 상태: 허용 파일 수정과 검증 완료. Git 메타데이터 쓰기 제한으로 커밋 없이 작업 트리에 남김.
- 반영 범위: 두 화면 스크립트, 지도 표시 데이터, 화면 검사 두 개, 자동 갱신한 `index.html` 주소 해시 세 곳, 이 인계 메모. 셸·다른 화면·보호 기록·옛 대조 파일·공통 검증기는 수정하지 않았다.

## 바꾼 것

| 파일·줄 | 변경 |
|---|---|
| `assets/app/js/screens/map.js:208`, `assets/app/js/screens/map.js:659` | 화면 제목을 작전 지도로, 탐색 이름표를 작전 지도 탐색으로, 목록 단추를 지도 목록으로 변경. 주소 `#map-room`과 세부 식별자는 유지. |
| `assets/app/js/screens/map.js:74`, `assets/app/js/screens/map.js:638` | 함께 맞춰 본 자료, 알 수 없음, 신호 목록, 지역 목록·지역 상세 지도, 작전 진행, 통신 기록, 기록 대기, 위치 공개 보류, 최근 본 좌표 등 승인된 화면 용어 적용. 호출부호·좌표·날짜·판정 값은 유지. |
| `assets/app/js/screens/map.js:107`, `assets/app/js/screens/map.js:618` | 판정 문서의 영문 속성 키를 기록 코드·문서 종류·보낸 곳·받는 곳·근거·기록 한계로 표시. 기록 코드만 간략 보기에서 숨기며 여섯 필드 값과 문서 본문은 그대로 표시. |
| `assets/app/js/screens/map.js:655` | 목록에서는 `hero`를 `undefined`로 두어 셸의 기본 대표 그림을 사용. 상세는 `false`. 기존 `true`는 셸에서 그림 객체로 취급되어 `src`를 얻지 못했다. 셸 파일을 바꾸지 않고 호출부만 맞춤. |
| `assets/app/js/screens/personnel.js:180`, `assets/app/js/screens/personnel.js:222`, `assets/app/js/screens/personnel.js:313` | 세계 기록·사건 목록, 작전 진행, 조직, 소속과 관계, 인물 파일에 기록된 사건 등 화면 말 정리. 인물의 이름·별칭·역할·소속·상태·경력·대가·기록 한계는 원문 유지. |
| `assets/js/data/map-screen-data.js:116`, `assets/js/data/map-screen-data.js:129` | 읽음 상태, 연도 알 수 없음, 관측 지점, 위치 공개 보류, 판정 기록 안내. 연도 안내는 “고른 해까지 날짜가 확인된 기록을 표시합니다. 연도를 알 수 없는 기록은 점선으로 남습니다.”로 문장을 풀어 씀. |
| `tools/verify-app.d/map.mjs:13` | 승인된 전체 문자열 11개(이름 변경 10개·표 머리 1개)만 옛 자료의 기대값에서 치환. 다른 문장·주소·분기·수치·저장값은 계속 원문과 완전히 대조. |
| `tools/verify-app.d/map.mjs:197`, `tools/verify-app.d/map.mjs:221`, `tools/verify-app.d/personnel.mjs:106` | 새 문구, 목록·상세의 대표 그림 옵션, 한글 판정 머리와 본문 보존, 인물 문구와 연결 주소를 실제 화면 함수로 검사하는 항목 3개 추가. |
| `index.html:136`, `index.html:159`, `index.html:162` | `stamp-assets.mjs`로 지도 표시 데이터·지도 화면·인물 화면의 주소 해시만 갱신. 해시를 제거하면 기준 파일과 동일함을 별도 확인. |

`assets/app/css/screens/map.css`, `assets/app/css/screens/personnel.css`, `assets/js/data/personnel-display-data.js`는 변경하지 않았다. 효과 줄임 선택자와 6명 접기는 이미 구현되어 있고, 인물 명부는 아래 실측에서 길이 조건을 충족한다.

## 승인된 이름 변경 전체 목록

아래 줄 번호는 작업 후 파일 기준이다. 한 필드에서 여러 이름을 바꾼 경우 전체 문장을 함께 적었다. `성위대`, 식별자, 주소 조각, 사실의 순서와 미확정 여부는 유지했다.

| 파일:줄·필드 | 전 | 후 |
|---|---|---|
| `assets/app/js/screens/map.js:450` · `synchronyIntel 표 머리` | 성채 종 장부 | 성채 타종 장부 |
| `assets/js/data/map-screen-data.js:10` · `canonBoundary.fixedFacts.2` | 성위대 지휘관이 남방 특수부대 출신이라는 기록과, 그가 생존해 있을 경우 처형하라는 명령문이 함께 회수됐다. | 성위대 지휘관이 혈맹 특수부대 출신이라는 기록과, 그가 생존해 있을 경우 처형하라는 명령문이 함께 회수됐다. |
| `assets/js/data/map-screen-data.js:16` · `canonBoundary.pendingFacts.2` | 남부 혈교 강경파의 전후 지휘권 | 남방 혈맹 강경파의 전후 지휘권 |
| `assets/js/data/map-screen-data.js:19` · `canonBoundary.lineageGuard` | 어느 선택도 남부 혈교를 우시노다 중앙 혈교의 확정 후계로 만들지 않으며, 데드존 혈교를 남부 지휘 아래 재편입시키지 않는다. 같은 표식·의식·일시적 교신은 동일한 지휘 계보의 증거가 아니다. | 어느 선택도 남방 혈맹을 혈교의 확정 후계로 만들지 않으며, 데드존 혈교를 혈맹 지휘 아래 재편입시키지 않는다. 같은 표식·의식·일시적 교신은 동일한 지휘 계보의 증거가 아니다. |
| `assets/js/data/map-screen-data.js:27` · `decisions.execute.unresolved` | 처형 명령의 원 발신자와 지휘관의 실제 충성은 확인되지 않았다. 남부 강경파가 전후 지휘권을 장악했다는 결론도 승인되지 않았다. | 처형 명령의 원 발신자와 지휘관의 실제 충성은 확인되지 않았다. 혈맹 강경파가 전후 지휘권을 장악했다는 결론도 승인되지 않았다. |
| `assets/js/data/map-screen-data.js:28` · `decisions.execute.consequence` | 현재 지도 사본에서 지휘망 붕괴와 소환진 재활성 위험을 추적한다. 남부 전체의 권력 승계는 미확정이다. | 현재 지도 사본에서 지휘망 붕괴와 소환진 재활성 위험을 추적한다. 혈맹 전체의 권력 승계는 미확정이다. |
| `assets/js/data/map-screen-data.js:38` · `decisions.detain.unresolved` | 지휘관이 남방 명령에 불복한 것인지, 더 깊은 침투를 위해 협조한 것인지는 판단할 수 없다. | 지휘관이 혈맹 명령에 불복한 것인지, 더 깊은 침투를 위해 협조한 것인지는 판단할 수 없다. |
| `assets/js/data/map-screen-data.js:49` · `decisions.cooperate.unresolved` | 남부 강경파의 공개 적대, 지휘관의 장기 충성, 데드존 혈교의 공식 가담 여부는 확정되지 않았다. | 혈맹 강경파의 공개 적대, 지휘관의 장기 충성, 데드존 혈교의 공식 가담 여부는 확정되지 않았다. |
| `assets/js/data/map-screen-data.js:50` · `decisions.cooperate.consequence` | 현재 지도 사본에 공동 차단선과 비인가 교신을 표시한다. 데드존 혈교가 남부 또는 U.A.C 지휘에 편입된 것은 아니다. | 현재 지도 사본에 공동 차단선과 비인가 교신을 표시한다. 데드존 혈교가 혈맹 또는 U.A.C 지휘에 편입된 것은 아니다. |
| `assets/js/data/map-screen-data.js:88` · `theaters.3.title` | 남부 쿠데타 | 남방 쿠데타 |
| `assets/js/data/map-screen-data.js:90` · `theaters.3.summary` | 남부 해안의 집단 소환과 성위대 침투가 한 작전으로 수렴한다. 지휘 계통은 이미 오염됐다. | 남방 해안의 집단 소환과 성위대 침투가 한 작전으로 수렴한다. 지휘 계통은 이미 오염됐다. |

기존 파일에는 조직을 가리키는 표현 두 곳이 이미 “남방 특수부대”, “남방 명령”으로 되어 있었다. 같은 승인 조직에 해당하므로 각각 “혈맹 특수부대”, “혈맹 명령”으로 통일했다. `우시노다 중앙 혈교`는 중첩된 옛 명칭 전체를 `혈교`로 정리했다. `남부 혈교를`의 조사도 `남방 혈맹을`로 고쳤다.

요청한 나머지 옛 이름은 이번 허용 파일에서 발견되지 않았다. 다른 표시 데이터에는 일부 남아 있으므로 아래 통합 메모를 참고한다.

## 옛 연출·기능과의 대응

이번 작업에서는 기존 연출의 속도·색·음원·타이머를 바꾸지 않았다. 아래는 그대로 유지한 대응과 문구 정리 지점이다.

| 옛 파일:줄 | 현재 파일:줄 | 대응 |
|---|---|---|
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:345` | `assets/app/js/screens/map.js:638` | 신호 검색·필터·위치 제한은 유지하고 색인 표지를 목록으로 정리. |
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:481` | `assets/app/js/screens/map.js:450` | 동시 관측 10곳과 장부 수는 유지. 성채 타종 장부·관측 지점·호출부호로 표 머리 정리. |
| `tools/fixtures/legacy-app/assets/js/pages/map-room.js:952` | `assets/app/js/screens/map.js:534` | 단계·시각·부대 상태·재생과 키보드는 유지. 작전 진행·통신 기록 표지 적용. |
| `tools/fixtures/legacy-app/assets/css/map-room.css:648`, `:659`, `:685` | `assets/app/css/screens/map.css:32`, `:35` | 선택·동시 관측 파동과 붉은 스캔 유지. 줄임 모드에서 선과 파동의 정적 형상을 남김. |
| `tools/fixtures/legacy-app/assets/css/personnel-archive.css:62`, `:81` | `assets/app/css/screens/personnel.css:4` | 인물 파일이 위에서 열리는 0.46초 효과 유지. 줄임 모드에서는 애니메이션만 중지. |
| `tools/fixtures/legacy-app/assets/js/pages/personnel-archive.js:215`, `:261` | `assets/app/js/screens/personnel.js:123`, `assets/app/js/screens/personnel.js:90` | 인물 상세 이동·검색·목록 복귀 유지. 소속별 6명 접기와 펼침 상태 유지. |

## 간략 보기에서 숨긴 것과 남긴 것

- 이번 추가 숨김: 현장 판정 문서의 내부 `FIELD VERDICT` 코드 필드. 전체 보기에서는 기록 코드라는 한글 표 머리와 함께 보인다.
- 유지한 숨김: 영문 겹표지, 내부 사건·작전·관측 코드, `PERSONNEL REGISTER`, 소속의 중복 코드, 장식용 채널 수치.
- 항상 남김: 이름·별칭·역할·주 소속·부 소속·상태·기준 연도·날짜·본문·이동 링크, 좌표·호출부호·판정·위험도·측정값·교전 규칙, 판정 문서의 보낸 곳·받는 곳·근거·한계.
- 명부는 전체 65명 중 54명으로 시작한다. 소속별 최대 6명이며 검색·상태·소속 필터를 쓰면 일치하는 인물 전원을 표시한다. 더 보기 단추 3개와 기존 펼침 상태 복원을 유지했다.

## 인물 목록 길이 실측

- 환경: Chrome `153.0.8010.53` headless, 1280×900 CSS px, 기본 배율, 간략 보기, 검색·필터 없음. 외부 Google Fonts 요청이 차단되어 같은 대체 글꼴로 대조했다.
- 이전 기준: A 작업 전 `0893c36`의 `personnel.js`·`personnel.css`만 메모리에서 제공하고, 나머지 셸·데이터는 현재 파일을 사용했다. 저장소에 과거 파일을 덮어쓰지 않았다.

| 조건 | 표시 인원 | 전체 페이지 높이 | 이전 대비 |
|---|---:|---:|---:|
| A 이전 화면·현재 셸 | 65명 | 10,365px | 100% |
| A-3 시작 상태·6명 접기 | 54명 | 5,163px | 49.81% |
| A-3 완료 상태·6명 접기 | 54명 | 5,163px | 49.81% |

절반 기준 5,182.5px 이하를 이미 충족하므로 행 높이·간격을 추가로 줄이지 않았다. 전체 보기는 6,116px로 측정됐다. 절반 기준은 기본 간략 보기에 적용했다.

## 확인한 것

- `node tools/stamp-assets.mjs`: 누적 3개 주소 해시 갱신. 최종 지도 표시 데이터 `0a9d89d0`, 지도 화면 `841f7e37`, 인물 화면 `712ddf00`.
- `node tools/verify-app.mjs`: **149/149 통과**. 기준 146개에 문구·대표 그림·본문 보존 검사 3개 추가. 셸 부재로 실패한 항목 없음.
- `node tools/verify-data.mjs`: **356/356 통과**.
- `git diff --check`: 공백 오류 없음. 줄바꿈 자동 변환 안내는 있으나 검증 실패는 아니다.
- 보호 원문·보호 기록 검사, 모든 데이터 로드, 4개 지휘 판단과 저장 복구, 순례 분기·10개 종결 판정·판정 보존·삭제·새로고침 대조 통과.
- 효과 줄임 검사는 두 화면 모두 `html[data-fx="reduced"]`를 요구하고 기존 시스템 미디어 쿼리를 금지한다. 선택적 셸 호출·리스너·타이머 정리 검사도 유지했다.
- 실제 브라우저: 9개 주소 × 1280px/375px × 간략/전체 = **36개 표시 조건**. 잘못된 DOM 문자열 없음. 1280px 문서 가로 넘침 없음. 375px는 아래 공용 바 문제를 확인했다.
- 375px의 18개 조건에서 두 화면의 `scrollWidth - clientWidth`는 모두 0이고, 화면 좌우가 뷰포트 안에 있다. 생성된 판정 상세도 두 해상도에서 본문 가로 넘침 없음.
- 확인 주소: 지도 목록·세계 지도·북부 지역·분산 계측 지점·불멸 작전·삼야 무응답, 인물 목록·사쿠마 유타·아마미야 레이. 별도로 귀환 판정 기록을 생성해 한글 머리를 확인했다. 검증용 새 브라우저 문맥만 사용했다.
- 인물 펼침 54→57명, 상세·뒤로·다른 화면 왕복 유지, 다시 접기.
- 인물 검색·빈 상태·초기화·Tab 포커스.
- 셸 보기 단추로 간략↔전체 변경.
- 지도 목록→지역→사진판→지점 상세→뒤로→지도 목록.
- 작전 이전·다음, 방향키, 스페이스 재생·정지.
- 시스템 줄임 자동 반영, 정적 지도·파동·경로 보존, 실제 설정 단추로 전체 우선·재생 정지, 인물 본문 보존.
- 새 판정 문서의 보낸 곳·받는 곳·읽음 상태, 데스크톱·모바일 본문 폭.
- 대표 그림: 지도 목록에는 등록된 그림이 실제로 로드됨. 지역·지점·작전·동시 관측·판정 상세와 인물 상세에는 채널 대표 그림 없음.
- 코드 수정 뒤 새로고침하여 최종 파일을 사용했다. 데스크톱 지도·인물 화면과 모바일 지역·인물 화면을 캡처로 육안 확인했다. 범위를 지키기 위해 별도 캡처 파일은 저장하지 않았다.
- 앱 자바스크립트 오류 0건. 콘솔 전체 무오류는 아님: Google Fonts CSS 요청 한 종류가 `net::ERR_NETWORK_ACCESS_DENIED`로 차단됐다. 새로고침 도중 배경 음원 요청 1건의 `net::ERR_ABORTED`도 관찰됐다.

## 확인하지 못한 것

- 원격 배포, 다른 브랜치와의 병합, 실물 휴대폰·Safari·Firefox, 실제 소리 청취·음량은 확인하지 않았다.
- 폰트가 정상 로드되는 환경에서의 높이·글꼴 모양은 확인하지 않았다. 현재 길이 수치는 동일한 대체 글꼴 조건의 결과다.
- 드문 이상 신호를 장시간 기다려 보는 검사와 감식 모달 전체 동작은 이번 A-3에서 다시 수행하지 않았다. 기존 자동 검사와 지도 사진판 열기는 확인했다.

## 셸·데이터 담당에게

1. **375px 공용 상단 바 4px 넘침**: 두 화면의 모든 모바일 조건에서 문서 폭은 379px였다. `[data-tc-settings]`의 “설정/표시” 자식 오른쪽이 약 379.14px까지 나온다. 본문은 343px 안에서 넘침이 없다. `assets/app/css/shell.css:462`의 모바일 바 배치, 특히 `:474`, `:481`의 간격·패딩·축소 조건을 검토할 것. 이번 작업에서 공용 바 또는 그것을 덮어쓰는 화면 CSS는 바꾸지 않았다.
2. **범위 밖 표시 데이터의 남은 문구**: `assets/js/data/channel-identity-data.js:24`의 “권역·현장…판독한다”, `assets/js/data/map-room-data.js:33`의 “권역 선택”은 현재 화면에도 나온다. 데이터 담당 용어 정리 때 반영할 것.
3. **범위 밖 지명·조직명**: `assets/js/data/regional-drilldown-data.js:16`의 “북부 연속 차단선”, `:64`의 “북해 피의 호수 봉쇄축”, `:118`의 “무등화 성채 종 운반 재구성”, `:161`의 “남부 혈교” 등이 남아 있다. 사용자 결정에 따라 Claude가 나머지 데이터에서 이름을 변경하는 범위다. 화면에서 임의 문자열 치환으로 감추지 않았다.
4. `map-screen-data.js`의 보고서 본문에 남은 교신·사본·발신자·지휘 계통 및 문서 고유 수신처 “단말 관제 기록”은 화면 용어 적용 대상이 아니므로 보존했다. 타종 장부 이름 외의 일반 지도 표식도 세력 문양이 아니므로 유지했다.
5. 지도 목록은 `hero:true`를 보내면 안 된다. 현재 셸은 참 값을 그림 객체로 읽는다. 지금처럼 목록에서는 옵션을 비우고 상세에서만 `false`를 전달하면 된다. 셸 수정과 새 음원 사건 제안은 추가하지 않았다.

## 커밋 상태와 반영 범위

현재 브랜치는 `codex/wording-map-personnel`이다. 작업 트리의 `.git`은 `C:/Users/FORYOUCOM/OneDrive/문서/GitHub/project-curse/.git/worktrees/codex-atm-a`를 가리킨다. 해당 메타데이터는 쓰기 허용 경로 밖이며, 이 세션의 Git 메타데이터 권한도 읽기 전용이다. “.git에 쓸 수 없으면 커밋하지 말고 작업 트리에 남긴다”는 요청에 따라 스테이징·커밋을 하지 않았다. 권한 확대나 다른 브랜치 변경·푸시도 하지 않았다.

쓰기 가능한 환경에서 아래 7개 파일을 현재 브랜치의 한 커밋에 포함한다.

- `assets/app/js/screens/map.js`
- `assets/app/js/screens/personnel.js`
- `assets/js/data/map-screen-data.js`
- `tools/verify-app.d/map.mjs`
- `tools/verify-app.d/personnel.mjs`
- `index.html`
- `HANDOFF-wording-map-personnel.md`

제안 커밋 메시지: `작전 지도와 인물 기록의 화면 문구 및 승인 이름 정리`

병합 때 셸·데이터가 함께 바뀌면 `stamp-assets.mjs`와 두 검증을 다시 실행한다. 이 작업은 공개 사이트에 배포하지 않았다.
