# 상황 관제·인물 기록 분위기 복원 인계

- 작업일: 2026-09-25
- 작업 브랜치: `codex/atmosphere-map-personnel`
- 기준 커밋: `0893c36`
- 대상 버전: Project Curse 6.0.0 새 단말
- 반영 범위: 아래 화면 파일·화면용 데이터·화면 검사와 `stamp-assets.mjs`가 갱신한 `index.html`의 주소 해시. 공용 셸, 사실 데이터, 보호 원문, 옛 앱 대조 파일은 수정하지 않았다.

## 바꾼 것

| 파일·줄 | 변경 |
|---|---|
| `assets/app/js/screens/map.js:91`, `:208`, `:215`, `:227` | 효과 모드 판단, 지형 SVG 위의 경로·파동 표시층, 관제 점, 붉은 스캔. 이미지에 움직임을 굽지 않고 `PCApp.h()`로 만든 요소를 움직인다. |
| `assets/app/css/screens/map.css:31` | 7초 스캔, 3.2초 관제 점 호흡, 2.2~2.8초 파동, 1.8초 점선 흐름, 0.28초·좌우 3px·1회 흔들림. 파동은 SVG 원이며 둥근 패널을 추가하지 않았다. |
| `assets/app/js/screens/map.js:106`, `:259`, `:265`, `:457` | 간략 보기용 표지 분리, 관측점 수와 생체 반응 수의 이상 신호 연결, 증거 감식 연결, 상세 화면의 대표 그림 제외. |
| `assets/app/js/screens/map.js:320`, `:422`, `:475`, `:516`, `:527` | 치명 선택·접근 거부 때 한 번 흔들림, 읽지 않은 판정의 새 기록 표시, 등록된 효과음 호출, `pc:fx` 수신, 명시된 작전 등급만 선택적으로 전달. 타이머·리스너는 `hide()`에서 해제한다. |
| `assets/app/css/screens/map.css:105` | 기본 간략 보기에서 코드 열도 함께 제거. 효과 줄임에서는 스캔·경로·파동을 없애지 않고 정지 상태로 둔다. |
| `assets/app/js/screens/personnel.js:18`, `:81`, `:269` | 소속별 첫 6명, `나머지 N명 보기`와 접기. 검색·소속·상태 필터를 쓰면 결과 전원 표시. 펼침은 메모리에만 보관하고 주소를 바꾸지 않는다. 상세 왕복·다른 화면을 다녀온 뒤에도 유지한다. |
| `assets/app/js/screens/personnel.js:134`, `:208`, `:321` | 사진 겹표지와 감식 식별값, 상세 이름의 이상 신호, 자유 해석 목록, 효과 모드 변경과 정리. 상세는 자체 표지를 쓰므로 `screenHead()`와 대표 그림을 추가하지 않는다. |
| `assets/app/css/screens/personnel.css:3`, `:21`, `:74`, `:89`, `:107` | 파일이 위에서 열리는 0.46초 효과, 짧은 명부 행, 이름·별칭과 역할·소속의 재배치, 모바일 한 열 배치, 효과 줄임의 정지 상태. |
| `assets/js/data/map-screen-data.js:125` | 지도 안내문 3개만 쉬운 말로 교체. 위치 보류의 뜻은 ‘승인된 지도 지점 없음’으로 유지했다. |
| `assets/js/data/personnel-display-data.js:7` | `이름을 누르면 주요 기록…` 안내와 이상 신호 표시 대상 3개의 식별자. 인물 사실 문장은 수정하지 않았다. |
| `tools/verify-app.d/map.mjs:96`, `:127`, `:198` | 효과 모드·운용 정보 보존·감식·치명 선택·새 판정·소리 검사. 화면 함수를 실제 실행하는 DOM 대역을 추가해 인물 검사에서도 쓴다. 배치 엔진 대용은 아니다. |
| `tools/verify-app.d/personnel.mjs:50`, `:62` | 승인된 안내문 변경만 허용하고 연도·상태·대가·기록 한계 원문 대조는 유지. 접기·검색·왕복·이상 신호·자유 해석·정리 검사 추가. |
| `index.html:34`, `:110`, `:124`, `:143` | 수정한 스크립트·스타일 6개의 내용 해시만 자동 갱신. 해시를 지우고 비교하면 기준 커밋의 파일과 동일하다. |

## 옛 연출과 대응

옛 경로는 모두 `tools/fixtures/legacy-app/` 아래에 있다. 원본은 변경하지 않았다.

| 옛 파일:줄 | 새 파일:줄 | 대응 |
|---|---|---|
| `assets/css/map-room.css:78`, `:988` | `assets/app/css/screens/map.css:33`, `:43` | LIVE 점을 단계식 깜빡임에서 느린 호흡으로 바꿈. |
| `assets/css/map-room.css:685`, `:991` | `assets/app/css/screens/map.css:32`, `:42` | 7초 붉은 스캔. |
| `assets/css/map-room.css:648`, `:659`, `:907` | `assets/app/js/screens/map.js:215`, `assets/app/css/screens/map.css:35` | 선택 표식과 동시 관측 지점의 파동. 동시 관측 10점을 이동 경로로 연결하지 않는다. |
| `assets/css/map-room.css:894`, `:992` | `assets/app/js/screens/map.js:208`, `assets/app/css/screens/map.css:37` | 실제 경로 좌표를 HTML 선분으로 옮겨 점선을 흐르게 함. 지형·기호는 기존 SVG를 유지한다. |
| `assets/css/pilgrimage-scenario.css:2`, `:64` | `assets/app/js/screens/map.js:399`, `:490`, `assets/app/css/screens/map.css:40` | 순례·귀환 심사·회수 경로와 현 지점 맥동, 치명 선택 뒤 1회 반응. 반복 화면 흔들림은 만들지 않았다. |
| `assets/css/verdict-archive.css:21`, `:25` | `assets/app/js/screens/map.js:113`, `:422`, `:539`, `assets/app/css/screens/map.css:39` | 읽지 않은 판정에 새 기록/NEW 표시, 잠긴 기록 접근 때 1회 거부 반응. |
| `assets/css/personnel-archive.css:62`, `:81` | `assets/app/js/screens/personnel.js:212`, `assets/app/css/screens/personnel.css:4` | 인물 파일의 위에서 아래로 열리는 효과. 화면 인계와 기동 효과는 셸 담당으로 남긴다. |

## 간략 보기에서 숨긴 것과 남긴 것

- 숨김: 한글 제목 옆 영문 절 표지, 사건·작전·관측 내부 코드, 권역 영문 표지, 소속의 `XXX / PERSONNEL REGISTER`, 각 명부 행의 중복 소속 코드, 채널 장식용 버전·개수, 매체 내부 식별자, 겹표지의 영어 부분.
- 유지: 이름·별칭·역할·주 소속·부 소속·상태·날짜·본문·모든 이동 링크, 작전 단계 시각, 좌표, 부대 호출부호, 판정·위험 태그, 규칙, 현장 측정값, 판정 잠금 해제에 필요한 코드.
- 지도 목록의 DOM 텍스트에서 영문 토큰은 전체 123개 → 간략 15개다. 정규식으로 센 대문자 토큰이며 접힌 신호 색인의 내용도 포함한다. 1280px 첫 화면에 실제 보이는 표지 개수를 실측한 값은 아니다.
- 인물 기본 목록은 65명 중 54명이다. 민간 증언망 3명·아크 2명·우시노다 6명을 접는다. 검색이나 필터는 접기 제한 없이 모든 일치 인물을 보여 준다. 펼침 단추에는 `aria-expanded`·`aria-controls`가 있고, 조작 뒤 포커스를 유지한다.
- 명부 행을 짧게 배치했지만, 기존 약 10,300px의 절반 이하인지 브라우저로 실측하지는 못했다.

## 데이터에 근거한 연결

- 인물 이상 신호는 상세의 일반 `span`에만 붙였다. 제목·링크·단추·목록 이름은 대상이 아니다.
- `reiki` 아마미야 레이: 개인 장소 기억이 흐려지는 대가.
- `apostle-luke-eugene` 루시앙 유진: 고유 기억과 신체 특징의 일시 소실.
- `apostle-alvarez` 마테오 알바레스: 원래 얼굴과 목소리를 재현하지 못하는 대가.
- 지도 이상 신호는 삼야 무응답의 관측점 수와 귀환 회랑의 생체 반응 `5개 검출`에만 붙였다. 별도 대체 숫자는 지어내지 않았다.
- 현재 작전 5개에는 셸에 전달할 명시적인 `critical/high/elevated/guarded/low` 등급이 없다. 따라서 현재 데이터로는 `PC.threat?.()`를 호출하지 않는다. `BLACK`, `ACTIVE INTELLIGENCE`, 부대 신호 소실을 위협 등급으로 추정하지 않았다. 명시 등급이 있는 검사 자료에서는 정확한 값을 전달함을 확인했다.
- 기존 지도 그림의 `확대·세계 기록 열기`는 감식과 이동이 섞인 동작이었다. 그림을 링크 밖 `.tc-evidence-media`에 두고 `세계 기록에서 보기` 링크를 별도로 남겼다. 확인된 매체 식별자를 `data-record`로 전달하고, 매체에 없는 DTG·장소·좌표는 넣지 않는다. 구역 그림에 선택 지점 좌표를 임의로 붙이지 않는다.
- 자유 해석은 `ProjectCurseOpenCanon.personnel[id]`가 있을 때만 나온다. `PC.openCanon?.(text)`를 사용하며, 자료만 먼저 들어온 경우에도 같은 목록 안에서 문구를 읽을 수 있다.

## 확인한 것

- `node tools/stamp-assets.mjs`: 수정한 스크립트·스타일의 주소 해시 갱신.
- `node tools/verify-app.mjs`: 125/125 통과.
- `node tools/verify-data.mjs`: 356/356 통과.
- `node --check` 지도·인물 스크립트: 통과.
- `git diff --check`: 공백 오류 없음.
- 보호 기록 해시, 데이터·매체 대장, 옛 상태 저장소와의 선택 분기·10개 종결 판정·재시작·판정 사본 대조 유지.
- DOM 대역에서 모든 지도 주소·인물 65개 상세, 없는 주소, 필터, 키보드 작전 이동, 재생 정지, 목록 왕복, 인물 펼침 유지, 중복 리스너 방지, 타이머·프레임 정리 확인.
- 시스템 줄임 + 셸 전체 조합에서 JS 재생 허용, `pc:fx` 줄임 전환 시 정지, 정지해도 경로·파동·내용 유지, 줄임 모드에서도 등록된 소리 사건 호출 확인.
- 375px용 한 열 배치, `minmax(0, 1fr)`·`min-width:0`·긴 글 줄바꿈, 장식 표시층의 지도 밖 넘침 차단을 코드로 확인했다.

## 확인하지 못한 것

- 브라우저 목록이 비어 있었고 검증용 탭도 `Browser is not available: iab`로 열리지 않았다.
- 실제 1280px·375px 렌더링, 가로 넘침 0, 인물 페이지 높이 절반 이하, 첫 화면의 영문 표지 개수, 실제 Tab 포커스 표시, 브라우저 뒤로 가기·해시 전환, 브라우저 콘솔 오류 0은 확인하지 못했다.
- 실제 음원 재생·음량, 셸 감식 화면 열기/닫기, 드문 이상 신호의 실제 표시, 전역 위협 반응과 자유 해석 자료의 통합도 확인하지 못했다. 테스트의 소리는 사건 이름 호출까지다.
- 정적 검사에서 셸 부재로 실패한 항목은 없다. 검사를 통과한 것이 아래 셸 통합 완료를 뜻하지 않는다.

## 셸 담당에게

1. 현재 `assets/app/css/base.css:108`의 전역 `@media (prefers-reduced-motion: reduce)`는 애니메이션을 1ms·1회로 강제한다. 셸의 `html[data-fx="reduced"]` 규칙으로 교체해야 시스템 줄임 환경에서 사용자가 ‘효과: 전체’를 고른 경우도 실제 화면에 반영된다. 이번 범위에서는 공용 파일을 수정하지 않았다.
2. `PCApp.fx()` 값을 갱신한 뒤 `document`의 `pc:fx`를 보내면 두 화면이 즉시 다시 판단한다. 공용 API가 없을 때는 시스템 설정으로 동작한다. 화면 CSS에는 기본 간략 보기와 효과 줄임의 범위 한정 대응을 넣었다.
3. 감식·위협·이상 신호·자유 해석은 요청된 공용 계약을 소비한다. 통합 후 `#map-room/region/deadzone-return-corridor`, `#map-room/synchrony/three-night-silence`, `#personnel/reiki` 등을 실제 브라우저에서 확인할 것.
4. 지도 상세는 `hero:false`가 되도록 호출한다. 인물 상세에는 `screenHead()` 호출 자체가 없다. 대표 그림 자동 삽입을 두 화면에서 중복 구현하지 않았다.
5. 추가 음원 사건 제안은 없다. `map.layer`, `archive.filter`, `system.denied`와 기존 단계·현장 절차 사건을 사용한다. 소리 기본 켜짐은 셸 설정을 따른다.
6. 지도·인물 화면 코드와 화면용 데이터 6개, 검사 2개, 해시 변경과 이 메모를 함께 검토한다. 공개 사이트 반영·다른 브랜치 병합·푸시는 수행하지 않았다.

## 커밋 상태

커밋은 만들지 못했다. 허용된 10개 파일을 명시해 `git add`를 실행했지만 아래 오류로 스테이징부터 거부됐다.

```text
fatal: Unable to create 'C:/Users/FORYOUCOM/OneDrive/문서/GitHub/project-curse/.git/worktrees/codex-atm-a/index.lock': Permission denied
```

현재 환경의 쓰기 허용 범위 밖에 Git 작업 트리 메타데이터가 있다. 권한 제한을 우회하지 않았으며, 파일 변경과 이 메모는 작업 트리에 남아 있다. 현재 브랜치는 계속 `codex/atmosphere-map-personnel`이다.

사용할 한국어 커밋 메시지: `상황 관제와 인물 기록의 연출을 복원하고 간략 보기를 정리`

쓰기 가능한 환경에서 위 10개 파일을 같은 커밋에 포함하면 된다. 정적 검증 결과는 이 커밋 실패와 별개로 모두 통과했다.
